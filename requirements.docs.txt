# ============================================================================
# 构建文档站点（MkDocs + Material for MkDocs）所需的 Python 依赖
# 用法：
#   CI   : pip install -r requirements.docs.txt
#   本地 : python -m venv .venv && .venv\Scripts\activate && pip install -r requirements.docs.txt
#
# 说明：
#   - 本文件必须保留在仓库根目录，GitHub Actions 的 setup-python@v5 才会在
#     cache: pip + cache-dependency-path: requirements.docs.txt 的组合下
#     命中缓存并避免 "No file matched to **/requirements.txt or **/pyproject.toml"
#     的报错。
# ============================================================================

# --- 站点生成器 & 主题 ---
mkdocs>=1.6,<2
mkdocs-material>=9.5,<10
mkdocs-material-extensions>=1.3,<2

# --- Markdown 扩展与代码高亮 ---
pymdown-extensions>=10.9,<11
pygments>=2.18,<3

# --- 插件 ---
mkdocs-minify-plugin>=0.8,<1    # HTML/CSS/JS 压缩
mkdocs-glightbox>=0.4,<1        # 图片点击放大
